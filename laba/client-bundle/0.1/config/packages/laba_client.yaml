laba_client:
    # Read-only Redis cache shared between services (needs ext-redis).
    # global_cache:
    #     dsn: '%env(REDIS_CACHE_DSN)%'
    #     timeout: 2.0
    #     cache_prefix: ''

    clients:
        # payment:
        #     class: Laba\PaymentClient\PaymentClient
        #     base_url: '%env(PAYMENT_API_URL)%'
        #     api_key: '%env(PAYMENT_API_KEY)%'
        #     # api_key_header: Authorization    # default: x-api-key
        #     # cache_enabled: false
        #     # name_converter: serializer.name_converter.camel_case_to_snake_case
